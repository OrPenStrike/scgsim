---
title: "Palace Electrostatic + SGB"
---

::: {.callout-note title="CONVERGING candidate contract"}
The backend, Route A/B lowering, handoff, Slurm execution, strict result
resolution, and report summaries have live candidate evidence. They remain
CONVERGING and are not V1 stable.
:::

## Candidate surface

The public backend object is `scgsim.palace.ElectrostaticSim`. Staged calls
expose geometry, terminals, solver configuration, mesh, config, handoff, and
result processing instead of hiding the workflow in one orchestration cell.

The intended visible sequence is:

```text
construct component
-> set geometry / stack / airbox
-> add exact-net terminals and physical grounds
-> configure Electrostatic
-> mesh / config
-> common handoff
-> resolve / results / report
```

Handoff preparation does not submit Palace. Generated local or Slurm scripts
execute explicitly and write a receipt that the resolver verifies. Slurm
scripts bind `#SBATCH --output` and `--error` to `logs/palace-%j.log` so the
scheduler does not also write `slurm-%j.out`.

Before a generated script starts the solve, the exact copied `config.json` is
passed to the selected Palace executable with its native `--dry-run` option.
Only a successful dry-run of those exact bytes establishes config/runtime
compatibility. The compatibility artifact and returned receipt preserve the
config hash and observed native GitTag. When the configured executable is
directly and truthfully resolvable as the Palace binary, its basename and
SHA-256 are also retained. SCGSim does not inspect behind a wrapper to discover
its hidden binary. GitTag, commit, and any available binary hash are provenance
for reproducibility and diagnosis; their presence or value is not a
runtime-eligibility Gate.

If this preflight fails, the returned receipt records
`execution_stage="config_compatibility"`, `solver_invoked=false`, its separate
preflight and pipeline exit codes, and `solver_exit_code=null`. Receipts use
`execution_stage="solver"` only after the solver command was actually invoked.

| Stage | Candidate method | Intended responsibility |
|---|---|---|
| Geometry input | `set_geometry` | Bind the public Component input to the SGB geometry route. |
| Stack input | `set_stack` | Bind the layer-stack input consumed by SGB. |
| Airbox | `set_airbox` | Declare the simulation-domain envelope before geometry production. |
| Terminal binding | `add_terminal` | Select a terminal by exact SGB net identity. |
| Physical Ground binding | `add_ground` | Select conductor components for Palace Ground by exact SGB net identity. |
| Problem controls | `set_electrostatic` | Declare Electrostatic-specific config controls. |
| Output identity | `set_output_dir` | Bind the mesh/config/handoff output location. |
| Mesh | `mesh` | Request the SGB-backed Palace mesh path. |
| Config | `write_config` | Write the Palace Electrostatic configuration for the reviewed mesh identities. |

These methods are reachable candidate APIs.

## Native multi-margin Surface EPR

The Electrostatic and Eigenmode `set_surface_epr(...)` specifications for each
MA, MS, or SA interface may include `inset_margins_um`. When present, its value
is an ordered, nonempty list or tuple of finite, nonnegative real values.
Request order is preserved, and equal values at different ordinals remain
distinct requested observations. Omitting the field preserves the existing
unmasked behavior.

For each exact source interface, SCGSim retains one logical unmasked baseline
and lowers every requested margin ordinal to a separate native masked
dielectric index that refers back to that baseline index. Masking does not
change the source interface attributes, type, thickness, relative
permittivity, or loss tangent. The generated native mask is
`{Type: Inset, Margin: ...}`.

Palace reads `Margin` in the same coordinate units as the input mesh: units of
`Model.L0`. SCGSim uses `Model.L0 = 1e-6 m`, so the configured input value is

$$
\mathrm{Margin}
= \frac{\texttt{margin\_um}\,10^{-6}\ \mathrm{m}}{\mathrm{Model.L0}}.
$$

Palace then nondimensionalizes both mesh coordinates and `Margin` by the same
relative mesh-length scale, $L_c / \mathrm{Model.L0}$. It does not divide the
configured `Margin` by `Model.L0` as a separate conversion.

At each retained pass, all baseline and masked coefficients are evaluated from
the same field solution; no margin requests another PDE solve. At quadrature
points, a positive `Inset` margin selects by distance from the perimeter of the
exact chosen surface patch. It does not alter the PDE, mesh, or notebook model.
A zero margin uses the native full-integration coefficient and must equal the
unmasked integration for the same interface. A positive margin requires an
open three-dimensional selected patch with a perimeter; the native
no-perimeter error remains a truthful failure rather than an approximated
result.

A complete masked snapshot requires both native files from the same retained
`final` or `iterationNN` result source:

- `surface-mask-Q.csv`, whose indexed columns are `p_surf_mask[Index]` and
  `Q_surf_mask[Index]`; and
- `surface-mask-energy.csv`, whose indexed columns are
  `E_surf_mask[Index] (J)`.

Both files use `m` as the first column for Eigenmode or `i` for Electrostatic.
If either file is missing or unreadable, the masked snapshot for that source is
unavailable; a valid baseline snapshot remains readable.

The index map and receipt bind every masked index to its exact baseline index,
interface owner and type, requested margin value and ordinal, mode or
excitation, refinement pass, and retained result source. Required masked files
are artifact-hash sealed for a completed result that requested masks; a hash
mismatch remains fatal. Repeated unmasked values or columns in native
`surface-Q.csv` are one logical baseline, not additional loss terms. Neither
repeated baselines nor alternative margins may be summed into the physical
loss total.

This multi-margin Surface EPR surface remains **CONVERGING**. It is not a
cumulative disjoint edge-band contract and introduces no scientific
acceptance, convergence threshold, or geometry-exclusion claim.

## SGB and Palace ownership

SGB is the only semantic geometry and topology authority. The first
implementation route is Route B:

```text
Component + layer stack
-> SGB XAO and structured physical-group records
-> Palace mesh and config
```

Route A follows through the same pipeline; it is not a second runner or
topology system. SCGSim owns only Palace-specific lowering, mesh/config,
handoff, resolve, and report behavior built on SGB identities.

For representation A, `route_a_thin_film="substrate_face"` preserves the
physical source Z coordinate. When the source supplies one typed physical
face-metal interval at the boundary of its explicitly declared vacuum host
solution, with exactly one adjacent typed dielectric, that single interval is
sufficient: SCGSim does not fabricate an opposing face or second substrate, and
it does not collapse the source coordinate. Its provenance records only the
physical side and sheet that exist. The inter-face gap is `null` and not
applicable; it is neither zero nor an inferred airbox distance.

Existing two-face `substrate_face` behavior is unchanged.
`metal_gap_equivalent` still requires two opposing physical face intervals.
The source `LayerStack` remains immutable. This single-face case changes no
mask, Port, Surface-EPR, or Route B semantics.

The candidate has no native-geometry fallback, physical-name semantic parsing,
replacement topology reconstruction, or external gdsfactory/cloud execution
fallback. Terminal selection is exact SGB net identity; legacy center/name
inference remains outside the contract.

## Candidate Electrostatic defaults

These values are **CONVERGING**, not accepted scientific defaults or Gates:

| Control | Candidate value |
|---|---|
| `save_fields` | `0` |
| `unassigned_conductor_policy` | `"ground"` |
| `exterior_boundary_policy` | `"none"` |
| `Model.Refinement.Nonconformal` | `false` unless the notebook sets `amr_nonconformal=True` |

Palace defaults `Nonconformal` to `true` when the key is omitted. SCGSim always
writes the key and defaults it to `false` (conformal tet AMR). Notebooks opt
into nonconformal AMR with `set_numerical(amr_nonconformal=True)`.

`add_ground(net_id=...)` is the explicit physical-conductor path. It is
independent of `exterior_boundary_policy="ground"`, which adds the finite
solution-domain exterior faces. With `unassigned_conductor_policy="error"`,
every conductor component must be covered by exactly one Terminal or explicit
Ground binding. The index map records physical-Ground and exterior-Ground
attributes separately and their exact union must equal
`Boundaries.Ground.Attributes`. Existing
`exterior_boundary_policy="none"` plus
`unassigned_conductor_policy="ground"` remains the implicit-ground path.
New Electrostatic authoring writes index-map schema v2. The resolver retains
schema-v1 read support for sealed historical runs, but only schema v2 carries
and can claim the physical-versus-exterior Ground split.

External gsim and historical SGB revisions are derivation evidence only. The
runtime authority is the in-tree `scgsim.sgb` Core and the current
`scgsim.palace` implementation.

See [Geometry and SGB Route A/B](../geometry-sgb.qmd),
[Headless Geometry Preview](../geometry-preview.qmd),
[Execution Profiles and Local/Manual-HPC Handoff](../execution-profiles.qmd),
and [Provenance and Data Boundaries](../provenance.qmd) for the surrounding
contracts.
